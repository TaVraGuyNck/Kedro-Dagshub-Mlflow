{
  "name": "Kedro + MLflow",
  "build": {
    "dockerfile": "Dockerfile.dev"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      },
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  },
  "postCreateCommand": "pip install -e . && pip install python-dotenv",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}"
}
